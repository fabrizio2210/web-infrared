pipeline {
  agent any
  stages {
    stage('Build') {
      steps {
        sh '/usr/bin/true'
      }
    }
    stage('Test') {
      steps {
        sh '/usr/bin/true'
      }
    }
    stage('Deploy') {
      steps {
        sh '/usr/bin/true'
      }
    }
  }
}